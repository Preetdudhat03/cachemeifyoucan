{
    "name": "codeforge-options-analytics-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install:all": "npm install --prefix frontend && pip install -r requirements.txt",
        "dev": "npm run dev --prefix frontend"
    }
}